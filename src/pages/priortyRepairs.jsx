import React from "react";

const PriortyRepairs = () => {
  /* const uniqueCategories = [
    ...new Set(appliances?.map(({ catergory }) => catergory)),
  ];


  return <div>
  {appliances &&
            uniqueCategories?.map((uniqueCategory, categoryIndex) => (
              <div key={categoryIndex} className="flex justify-evenly ">
                <h3 className="w-24">{uniqueCategory}</h3>

                {appliances
                  .filter((appliance) => appliance.catergory === uniqueCategory)
                  .map((filteredAppliance, applianceIndex) => (
                    <ApplianceCard
                      key={applianceIndex}
                      jobnumber={filteredAppliance.jobnumber}
                      model={filteredAppliance.model}
                      brand={filteredAppliance.brand}
                      location={filteredAppliance.location}
                      priorty={filteredAppliance.priorty}
                      engineer={filteredAppliance.engineer}
                      created={filteredAppliance.created}
                      appliance={filteredAppliance}
                      status={filteredAppliance.status}
                    />
                  ))}
              </div>
            ))}
  </div>;*/
};

export default PriortyRepairs;
